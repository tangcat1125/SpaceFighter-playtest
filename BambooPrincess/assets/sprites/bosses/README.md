# Boss Sprites

All boss sprites use the same pipeline:

- `sheet_greenscreen_*.png`: source (solid #00FF00 background)
- `sheet_keyed_*.png`: runtime (offline keyed to alpha)
- `sprite_manifest.json`: frame ids and grid mapping

Frame size: `256 x 256`.

